multiply = '-' * 30
print(multiply)

upper_case = 'hello'.upper()
print(upper_case)

# if we want to trim any string we use the strip() method:
trim_string = ' abc   and trim    '.strip()
print(trim_string)

#we can also trim from start or end with argument
trim_start_and_end = 'abc'.strip('ac')
print(trim_start_and_end)

